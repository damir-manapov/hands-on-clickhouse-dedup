/**
 * Public exports for the hands-on-clickhouse-dedup playground.
 *
 * Most of the project lives under `scripts/` and `sql/`; this file exists
 * mainly to give `tsc` and `vitest` a stable entry-point.
 */

export const PROJECT_NAME = "hands-on-clickhouse-dedup";
